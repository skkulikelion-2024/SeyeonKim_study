// whole-script strict mode syntax
// JavaScript is very flexible, but also dangerous
// --> use strict, added in ECMAScript 5, blocks insane mistakes
'use scrict';

let a;
a=6; // when not using 'use strict';, simply saying a=6 causes error b/c variable a undefined

console.log('Hello World!');


